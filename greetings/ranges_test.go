package greetings

import (
	"testing"
)

// TestRange1 tests the Range1 function.
// Since Range1 only prints to the console, we can't directly test its output.
// However, we can check if it runs without panicking.
func TestRange1(t *testing.T) {

	Range1()
}
